apiVersion: v1
kind: Service
metadata:
  name: shadowmesh-gateway
  labels:
    app: shadowmesh-gateway
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: shadowmesh-gateway
