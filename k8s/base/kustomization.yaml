apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  - ingress.yaml
  - rbac.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/name: shadowmesh-gateway
  app.kubernetes.io/part-of: shadowmesh
